<!DOCTYPE html>
<html lang="en">
      
    <%- include('./partials/head.ejs'); -%>   <body> 
    
    <main class="flex-vertical">
        <div class="flex-vertical">
            <% for( let i = 0; (listar.length - i) > 0; i++) { %>
                <table>
                    <tr>
                        <td><p><strong><%= usuarios[i].idUsr %>)_ </p></strong></td>
                        <td><p><strong><%= usuarios[i].usuario %></p></strong></td>
                        <td><p><strong><%= usuarios[i].email %></p></strong></td>
                        <td><p><strong><%= usuarios[i].id_perfil %></p></strong></td>
                        <td><p><strong><%= usuarios[i].id_intereses %></p></strong></td>
                       
                    </tr>
                </table>
            <% } %>
        </div>
    </main>
    
    <%- include( './partials/footer.ejs' ); -%>

</body>
</html>